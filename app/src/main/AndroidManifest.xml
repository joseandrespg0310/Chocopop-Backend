<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Agregar el permiso de Internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChocoPop"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Actividad de inicio (Main) -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Actividad que se lanza al iniciar la app -->
        <activity
            android:name=".MainXmlActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Agregar la nueva actividad de registro -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />

        <!-- Otra actividad, por ejemplo el Dashboard -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true" />

    </application>

</manifest>
